# 세션 인수인계 — 2026-06-06 (칸반 1a-2 + ktalk 재시작 + hermes ERP 읽기 게이트웨이)

> ERP(tshirt-management, 서버 naxer-main docker `data.naxer.shop`) + ktalk(`D:\000_Share_AKINO\AKINO\Ktalk`) + hermes(WSL LLM 에이전트) 연동.
> 2개 세션 누적 정리. 후반부(§3~)가 최신 상태.

---

## 1. 한 일 (시간순)

| # | 작업 | 커밋/결과 |
|---|---|---|
| 0 | ktalk 실제 경로 정정 (`D:\Ktalk` → `D:\000_Share_AKINO\AKINO\Ktalk`) | CLAUDE.md·메모리 |
| 1 | 6/5 미커밋 ERP 작업 소급 커밋 (주소 자동등록·발송통보) | `7cfff1c` |
| 2 | 주문 단계 세분화: 결제·제작중 사이 `제작준비(PREP)` 추가 | `7cfff1c`·models 0024 |
| 3 | 칸반 7칸 정식 세분화 (대기중/상담중/제작준비/제작중/발송/결과통보/보류) | `2bcf960` 등 |
| 4 | 칸반 카드 인터랙션 (우클릭 메뉴·긴급 빨강·주소모달) | `d9ab220`·`6c8cafc`·`7a1c11b` |
| 5 | 칸반 반응형 UI (카드수 비례 가로펼침) | `13e496a` 등 |
| 6 | 카톡 연동 1a-2 ①②③ (§2) | ERP `5f5958d`·`34fd982`·`c79306d` / ktalk `5e357aa` |
| **7** | **ktalk 데몬 재시작** (04:03) — 새 워커 코드 로드 확정 (§4) | 운영자 직접 |
| **8** | **hermes ERP 읽기 게이트웨이** 신설·배포·검증·정리 (§3) | ERP `49d1963` |
| **9** | **정식 `--build` 배포** → 칸반 1a-2 임시반영을 정식 이미지로 흡수 | docker hub 회복 |

---

## 2. 카톡 연동 1a-2 구조 (직전 세션, 유지)

**아키텍처**: 서버(ERP)↔로컬(ktalk) 분리라 DB 직접 읽기 불가 → **폴링 브릿지**(ktalk → ERP push).

- **① 안읽음**: ktalk `kanban_sync_worker` (`list_chats_with_ids` + kakao.db 응대여부) → ERP `/orders/ktalk/kanban/sync/` push. 대기중=미응대, 상담중=응대후(복귀X), unread 빨간 배지
- **② 카톡창**: ERP `KakaoOpenRequest` 큐 + ktalk `open_request_worker`(3초) → `open_chat_by_id`. 주문매칭 `display_name ↔ Order.kakao_chat_name` → `Order.kakao_customer_id`
- **③ 매칭실패**: `.kc-unmatched`(주황 점선) + 새로고침 버튼
- 모델/마이그레이션: `KakaoConsultCard`(0025), `unread_count`(0026), `KakaoOpenRequest`(0027), `Order.kakao_customer_id`(0028) — **서버 DB 적용 완료**

---

## 3. hermes ERP 읽기 게이트웨이 (이번 세션 신규) ✅

**목적**: 별도 운영 hermes(WSL LLM 에이전트)가 ERP 자료 조회. **Phase 1 = 읽기 전용** (쓰기는 정기 백업 체계 확립 후, LLM full CRUD+DELETE 위험 단계화).

- **엔드포인트**: `POST https://data.naxer.shop/hermes/query/`, 헤더 `X-Hermes-Key: <HERMES_API_KEY>`
- **키 보관**: 서버 `.env.docker` (ktalk 키와 분리). ⚠️ compose env_file = **`.env.docker`** — compose 변수치환용 `/opt/tshirt/.env`에 넣으면 컨테이너 주입 안 됨 (이번에 실수→수정)
- **코드**: `tshirt_management/hermes_views.py` + root `urls.py` `/hermes/query/`. `apps.get_model()` 동적조회, 비즈니스 모델 15개. **쓰기 함수 자체 없음 = 구조적 read-only**. limit 상한 500
- **차단(403)**: `settings_app.APISettings`(자격증명) + `auth/sessions/admin/contenttypes`(비번해시·세션키)
- **요청 body**: `{model:"orders.Order", filters, exclude, fields, order_by, limit, offset}`
- **안내서**: `tshirt-management/HERMES_API_GUIDE.md` (hermes 전달용)
- **검증 통과**: 401(키없음)/200(orders total 636·products 47)/403(차단)/405(GET·PUT). `action:"delete"` 보내도 무시·조회만·데이터 불변 실증
- **hermes 측 확인**: 운영자가 hermes에서 200 정상 조회 확인 완료

---

## 4. 현재 상태

- **ERP git**: `49d1963` (hermes 게이트웨이) + 본 인수인계서 커밋. working tree 정리됨
- **ktalk git**: `5e357aa` (변경 없음, 재시작만)
- **서버 배포**: ✅ **정식 `--build` 완료** — 칸반 1a-2 + hermes 게이트웨이 모두 이미지에 박힘. 임시반영(docker cp+HUP) 해소. `tshirt-pending-rebuild` 숙제 종료
- **ktalk 데몬**: ✅ 재시작됨(04:03:58). 코드 커밋(03:49)보다 늦게 떠서 **새 워커(kanban_sync·open_request) 코드 로드 확정**. ⚠️ **단, 워커 실작동 실증은 미완** (로그 콘솔출력이라 미확인 — 상담 모니터/콘솔 로그 확인 필요)
- **hermes 게이트웨이**: ✅ live, 검증 통과

---

## 5. 다음 세션 할 일 (우선순위)

1. **ktalk 데몬 워커 실작동 검증** (홀드 중) — 콘솔 로그 `...칸반동기화·카톡창열기 폴링 워커 가동` 확인 + 상담 모니터에서:
   - 안읽음 고객 대기중/상담중 + 빨간 배지 / 카드 클릭→카톡창(~3초) / 주문매칭(주황점선) / 새로고침
2. **hermes Phase 2 (쓰기)** — 정기 백업 체계 확립 **후** 개통. 그 전제로 ERP DB 백업 현황 파악부터
3. **hermes 측 정리** — 키를 임시폴더 → `~/.hermes/.env`로 이동
4. **④ 발송결과 스크롤 버그** — "발송결과 보낼 때 스크롤 안 해 고객 못 찾음" 화면 위치 미확인. 운영자 확인 필요
5. **1a-2b 잔여** — 카드 클릭 분기(주문생성/상담완료)·재방문 복귀 미구현
6. **칸반 레이아웃** 운영자 최종 확정 안 됨 (masonry→가로펼침 임시 정착)

---

## 6. 주의/함정

- **hermes env_file = `.env.docker`** (compose 변수치환용 `.env` 아님). 키 추가는 `.env.docker`에 줄 단위 ([[erp-env-docker-divergence]])
- **ERP 배포**: scp → `--build --force-recreate`. 코드는 이미지에 박힘(볼륨 마운트 아님), gunicorn이라 자동 reload 없음. docker hub 회복 시 정식 빌드가 정석
- **ktalk 코드는 로컬**: scp 불필요, 데몬 재시작만. 단 재시작 = 운영자 손
- **KTALK_API_KEY / HERMES_API_KEY**: 각 양쪽 .env 동일값 (인증). 서로 분리된 키
- 배포 = ktalk→ERP 폴링 브릿지 ([[ktalk-erp-bridge-runtime]]). 데몬 켜져있어야 작동
- 도구 호출 형식: `invoke` 형식 오류 시 끊김 — 정식 형식 엄수 ([[tool-call-format-discipline]])
